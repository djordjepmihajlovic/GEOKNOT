#include <vector>
#include <cmath>
#include <iostream>

// Want to rewrite the computationally heavy stuff here
// writhe calculation
// parallelization of BFACF moves

double lattice_writhe_cimasoni(const std::vector < std::vector < std::vector < int>>> & array,
                                const std::vector<std::vector<double>>& projections) {

                                    double writhe = 0.0;
                                }